version: '3.7'
services:
    postgres:
        image: postgres:11
        ports:
          - "5432:5432"
        environment:
          - POSTGRES_DB=trivia
          - POSTGRES_USER=flaskr
          - POSTGRES_PASSWORD=trivia-app
